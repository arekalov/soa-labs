# Деплой ЛР1 (содержит личные пути, живёт только локально)
deploy.sh

# Секреты: приватные ключи и хранилища не должны попадать в репозиторий никогда.
# Публичные сертификаты (certs/*.crt) коммитить можно и нужно.
*.p12
*.jks
*.keystore
*.truststore
secrets/
deploy.env

# Сборка
build/
.gradle/
.kotlin/
node_modules/
client/dist/

# IDE
.idea/
*.iml
out/
